def setup(): 
    size(1200,600)
def draw():
        print(height,width,mouseX,mouseY,mousePressed)
        if mousePressed:
            print(height,width,mouseX,mouseY,mousePressed)
            circle(mouseX,mouseY,25)
        else:
            print(height,width,mouseX,mouseY,mousePressed)
        rect(182,height/2,height/10*3,height/10*3) # w programowaniu wizualnym, tam gdzie się da lepiej używać zmiennych zależnych
#1,75p

        
 
